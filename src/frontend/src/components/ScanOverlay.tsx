/**
 * ScanOverlay — disabled.
 * The full-screen scan overlay has been removed per user request.
 * Glow effects are now handled locally on buttons/cards (directional L→R).
 */
export default function ScanOverlay() {
  return <></>;
}
